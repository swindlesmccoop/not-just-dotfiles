#!/bin/sh
#Prints battery percentage and corresponding emoji

#PERCENT="$(apm | sed 's/Battery state:.*, //g' | sed 's/ remaining.*//' | head -n 1)"
PERCENT="$(apm | grep -o '[0-9].*%')"
[ "$PERCENT" = "" ] && exit
echo "$PERCENT"
