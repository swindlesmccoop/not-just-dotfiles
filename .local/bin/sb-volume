#!/bin/sh
sndioctl -n output.level | sed 's/0\.//' | sed 's/.$/%/' | sed 's/\.//'
#VOL="$(sndioctl -n output.level | sed 's/0\.//' | sed 's/.$//')"
#[ "$VOL" = "1.00" ] && echo "VOL: 100%" || echo "VOL: $VOL%"
#kill -s SIGUSR1 $(pgrep dwmblocks)
