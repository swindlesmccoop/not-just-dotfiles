#!/bin/sh

_openbsd() {
	sndioctl -n output.level | sed 's/0\.//' | sed 's/.$/%/' | sed 's/\.//'
}

_linux() {
	printf "$(pamixer --get-volume)%%\n"
}

OS=$(uname)
case $OS in
	Linux) _linux ;;
	OpenBSD) _openbsd ;;
esac
