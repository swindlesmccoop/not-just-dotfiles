#!/bin/sh

[ -z "$1" ] && exit

[ $(echo "$1" | grep .png) ] && sxiv "$1" && exit
[ $(echo "$1" | grep .jpg) ] && sxiv "$1" && exit
[ $(echo "$1" | grep .gif) ] && sxiv "$1" && exit

[ $(echo "$1" | grep .psd) ] && gimp "$1" && exit
[ $(echo "$1" | grep .xcf) ] && gimp "$1" && exit

vim $1
