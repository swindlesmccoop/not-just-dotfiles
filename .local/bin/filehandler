#!/bin/sh
[ $# -eq 0 ] && echo 'Usage: filehandler (file)' && exit
[ -z $1 ] && echo 'Usage: filehandler (file)' && exit
case "$1" in
	*.png) sxiv "$1" ;;
	*.jpg) sxiv "$1" ;;
	*.gif) sxiv "$1" ;;
	*.psd) gimp "$1" ;;
	*.xcf) gimp "$1" ;;
	*) vim "$1" ;;
esac
